# Operations

Maintenance the runtime never does on its own. The `retold` CLI is a thin front over this class; the [low-level design](../agent-memory-lld.md#16-operations-and-cli) lists every command.

::: retold.operations
