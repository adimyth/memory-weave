# Configuration

The keys an integrator is likely to touch. The full reference with the reasoning behind each value is [section 2 of the low-level design](../agent-memory-lld.md#2-configuration).

{%
  include-markdown "../../README.md"
  start="## Configuration"
  end="## What the numbers say"
  heading-offset=-1
  rewrite-relative-urls=true
%}
