# Configuration

Every key an integrator is likely to touch. The full reference with the reasoning behind each value is [section 2 of the low-level design](../agent-memory-lld.md#2-configuration).

{%
  include-markdown "../../README.md"
  start="## 4. Configuration"
  end="## 5. What has been measured"
  heading-offset=-1
  rewrite-relative-urls=true
%}
